export type PuzzleFormat = 'jwt-vuln' | 'saml-tamper' | 'protocol-guess'

export interface DailyPuzzle {
  id: string
  format: PuzzleFormat
  prompt: string
  /** Only used by 'protocol-guess' — clues revealed one at a time, most obscure first. */
  clues?: string[]
  choices: string[]
  correctIndex: number
  explanation: string
}

// A rotating bank of daily puzzles across 3 formats. 36 entries gives every visitor a
// distinct puzzle for a full 36-day cycle before any repeat (see dailyPuzzle.ts's
// date-seeded, deterministic index — no Math.random()).
export const DAILY_PUZZLE_BANK: DailyPuzzle[] = [
  // --- jwt-vuln ---
  { id: 'jwt-1', format: 'jwt-vuln', prompt: 'A server decodes a JWT and finds header `{"alg":"none","typ":"JWT"}`. What vulnerability does this expose?', choices: ['Weak HMAC secret', 'Signature bypass — the server may accept the token with no signature check at all', 'Missing "exp" claim', 'Token replay attack'], correctIndex: 1, explanation: 'Some vulnerable JWT libraries historically honored `alg: none` and skipped signature verification entirely, letting an attacker forge any payload.' },
  { id: 'jwt-2', format: 'jwt-vuln', prompt: 'A JWT is signed with HS256 using the secret `"secret123"`. What is the primary risk?', choices: ['The token cannot be decoded', 'A weak, guessable secret lets an attacker brute-force it and forge valid signatures', 'HS256 is deprecated', 'The token has no payload'], correctIndex: 1, explanation: 'HS256 security depends entirely on secret strength — a short, common secret can be cracked offline via dictionary attack, after which an attacker can sign arbitrary tokens.' },
  { id: 'jwt-3', format: 'jwt-vuln', prompt: 'A JWT payload has no `exp` (expiration) claim. What is the risk?', choices: ['The token is invalid JSON', 'The token never expires and remains valid indefinitely if leaked', 'The signature will fail to verify', 'The issuer cannot be identified'], correctIndex: 1, explanation: 'Without an expiration claim, a stolen token stays valid forever — there is no time-boxed blast radius if it leaks.' },
  { id: 'jwt-4', format: 'jwt-vuln', prompt: 'A server trusts the `kid` (Key ID) header to look up a verification key on disk, using the header value directly as a file path. What attack does this enable?', choices: ['Cross-site scripting', 'Path traversal / arbitrary file read, potentially loading a key the attacker controls', 'SQL injection', 'Denial of service'], correctIndex: 1, explanation: 'Untrusted `kid` values used directly in file/DB lookups let an attacker point verification at a key they control (e.g. `../../dev/null` or an attacker-supplied public key), bypassing signature checks.' },
  { id: 'jwt-5', format: 'jwt-vuln', prompt: 'A server configured to verify RS256-signed tokens is also willing to accept HS256 tokens, using the RSA public key as the HMAC secret. What attack does this enable?', choices: ['Token expiry bypass', 'Algorithm confusion — an attacker signs a forged HS256 token using the (public!) RSA key as the HMAC secret', 'Header injection', 'Replay attack'], correctIndex: 1, explanation: 'RSA public keys are, by definition, public. If a server accepts both algorithms and reuses the same key material, an attacker can sign their own HS256 token with the public key and pass verification.' },
  { id: 'jwt-6', format: 'jwt-vuln', prompt: 'A JWT contains `{"role":"admin"}` in its payload but the server never checks the signature before trusting the payload. What is the flaw?', choices: ['The payload is too large', 'Authorization is being derived from unverified, attacker-controlled claims', 'The token format is wrong', 'The role claim is misspelled'], correctIndex: 1, explanation: 'A JWT payload is just base64url-encoded JSON — anyone can edit it. Trusting claims without first verifying the signature means an attacker can grant themselves any role.' },
  { id: 'jwt-7', format: 'jwt-vuln', prompt: 'An access token and a refresh token are both stored in `localStorage`. What is the primary risk this introduces?', choices: ['Tokens expire too quickly', 'Any XSS vulnerability on the page can read both tokens via JavaScript', 'The tokens are too large', 'CORS will block the request'], correctIndex: 1, explanation: '`localStorage` is readable by any JavaScript running on the page — a single XSS bug lets an attacker exfiltrate both tokens, unlike an HttpOnly cookie.' },
  { id: 'jwt-8', format: 'jwt-vuln', prompt: 'A JWT\'s `aud` (audience) claim is never validated by the resource server. What attack does this enable?', choices: ['Token replay across services — a token minted for Service A can be replayed against Service B', 'Signature forgery', 'Clock skew errors', 'Base64 decoding errors'], correctIndex: 0, explanation: 'The audience claim scopes a token to its intended recipient. Without checking it, a token legitimately issued for one service can be reused against a completely different one.' },
  { id: 'jwt-9', format: 'jwt-vuln', prompt: 'A JWKS endpoint is fetched over plain HTTP instead of HTTPS. What is the risk?', choices: ['Slower response times', 'A man-in-the-middle can substitute their own public keys, letting forged tokens verify successfully', 'The JWKS format becomes invalid', 'CORS errors'], correctIndex: 1, explanation: 'If the JWKS fetch isn\'t protected by TLS, an on-path attacker can swap in their own key set, so tokens signed with an attacker-controlled key will pass verification.' },
  { id: 'jwt-10', format: 'jwt-vuln', prompt: 'A password-reset JWT and a login-session JWT are signed with the exact same secret and have identical claim structures. What is the risk?', choices: ['Faster verification', 'A password-reset token could be replayed as a valid session token (or vice versa) since nothing distinguishes their purpose', 'Larger token size', 'Base64 padding errors'], correctIndex: 1, explanation: 'Without a claim identifying the token\'s intended purpose (e.g. a `scope` or `typ` claim), a token minted for one narrow purpose can be misused for another.' },
  { id: 'jwt-11', format: 'jwt-vuln', prompt: 'A JWT is issued with a 30-day expiration and there is no server-side revocation mechanism. What is the operational risk?', choices: ['The token is too small', 'A compromised token stays valid for up to 30 days with no way to force it invalid before then', 'The signature algorithm is weak', 'Clock skew will reject it immediately'], correctIndex: 1, explanation: 'Stateless JWTs can\'t be revoked by design unless the server checks a denylist/allowlist — a long expiry with no revocation path means a leaked token is a long-lived risk.' },
  { id: 'jwt-12', format: 'jwt-vuln', prompt: 'A mobile app embeds a long-lived JWT signing secret directly in the compiled app binary. What is the flaw?', choices: ['The app will run slower', 'The secret can be extracted via reverse-engineering, letting anyone forge valid tokens', 'The JWT format becomes incompatible', 'It only affects Android, not iOS'], correctIndex: 1, explanation: 'Anything shipped inside a client binary should be treated as public — decompiling a mobile app to extract an embedded secret is straightforward, defeating the whole point of a signature.' },

  // --- saml-tamper ---
  { id: 'saml-1', format: 'saml-tamper', prompt: 'A SAML assertion\'s `<NameID>` field was changed from "alice@corp.com" to "admin@corp.com" after the signature was applied, and the SP still accepted it. What went wrong?', choices: ['The SP checked expiry but not the signature over the modified element', 'The IdP issued two tokens', 'The NameID format was invalid XML', 'TLS was not used'], correctIndex: 0, explanation: 'If the SP doesn\'t validate that the signed portion of the assertion actually covers the element being read (or skips signature validation on that subtree), a tampered field slips through — a classic XML Signature Wrapping variant.' },
  { id: 'saml-2', format: 'saml-tamper', prompt: 'An attacker copies a valid, signed `<Assertion>` element and wraps it inside a new, attacker-crafted `<Response>` with a different, forged assertion as the "visible" one the SP actually reads. What is this attack called?', choices: ['Golden SAML', 'XML Signature Wrapping (SAML Signature Wrapping / SSW)', 'Replay attack', 'Cross-Site Request Forgery'], correctIndex: 1, explanation: 'Signature Wrapping exploits SPs that verify a signature exists somewhere in the document but then process a *different* element than the one that was actually signed.' },
  { id: 'saml-3', format: 'saml-tamper', prompt: 'A SAML Response\'s `<Conditions NotOnOrAfter="...">` timestamp was stripped out entirely, and the SP accepted the assertion anyway. What class of bug does this represent?', choices: ["Missing expiry enforcement, allowing indefinite replay of an old assertion", 'A base64 encoding error', 'A namespace collision', 'An unrelated CSRF bug'], correctIndex: 0, explanation: 'If an SP doesn\'t enforce the presence and validity of time-bound conditions, a captured assertion can be replayed long after it should have expired.' },
  { id: 'saml-4', format: 'saml-tamper', prompt: 'An attacker who compromised an IdP\'s private signing key uses it to mint fully valid, correctly-signed SAML assertions for any user, without ever touching the IdP\'s login flow. What is this attack called?', choices: ['Golden SAML', 'Kerberoasting', 'Pass-the-Hash', 'DNS Spoofing'], correctIndex: 0, explanation: 'Golden SAML (used in the SolarWinds breach) means the attacker doesn\'t need to exploit the IdP\'s authentication logic at all — they forge trusted, cryptographically valid tokens directly using the stolen signing key.' },
  { id: 'saml-5', format: 'saml-tamper', prompt: 'A SAML assertion\'s `<AudienceRestriction>` was removed, and the SP accepted an assertion that was actually minted for a completely different service provider. What went wrong?', choices: ['The SP failed to verify the assertion was actually intended for it', 'The assertion had an invalid signature', 'The NameID was malformed', 'The IdP metadata expired'], correctIndex: 0, explanation: 'Audience restriction scopes an assertion to one specific SP. Skipping this check means an assertion legitimately issued for Service A can be replayed against Service B.' },
  { id: 'saml-6', format: 'saml-tamper', prompt: 'A `<AttributeStatement>` listing `role=viewer` was modified in transit to `role=admin`, but the digital signature still validated successfully. How is this possible?', choices: ['The signature only covers a subset of the document that excludes the AttributeStatement', 'SAML does not support attributes', 'The IdP does not sign assertions', 'XML cannot contain attributes'], correctIndex: 0, explanation: 'If the enveloped signature\'s reference/transform doesn\'t actually cover every attribute the SP later trusts, an attacker can modify unprotected elements while the signature over the (smaller) protected subset still checks out.' },
  { id: 'saml-7', format: 'saml-tamper', prompt: 'A SAML `InResponseTo` field, meant to tie a response back to a specific request the SP initiated, was left completely unvalidated by the SP. What attack does this enable?', choices: ['A captured, legitimate SAML Response can be replayed against the SP outside of any request the SP actually made', 'The XML becomes malformed', 'TLS certificate errors', 'The IdP metadata becomes invalid'], correctIndex: 0, explanation: '`InResponseTo` binds a response to the specific AuthnRequest that triggered it. Without validating it, an attacker can replay a previously captured, legitimately-signed response at any time.' },
  { id: 'saml-8', format: 'saml-tamper', prompt: 'An SP trusts SAML assertions from any IdP whose metadata it has ever seen, without pinning to a specific expected issuer. What risk does this create?', choices: ['Slower login times', 'A rogue or compromised IdP in the trust list could impersonate any user for that SP', 'XML parsing errors', 'Session timeout issues'], correctIndex: 1, explanation: 'Without pinning the expected issuer, any IdP the SP has ever trusted (even one added for a completely different purpose) could mint assertions the SP will accept.' },
  { id: 'saml-9', format: 'saml-tamper', prompt: 'A SAML assertion was intercepted over an unencrypted HTTP connection and replayed later by an attacker with no modification at all. What single control would have prevented reuse?', choices: ['A one-time-use restriction (e.g. tracked assertion ID) combined with a short validity window', 'A longer NameID', 'Removing the signature', 'Base64 double-encoding'], correctIndex: 0, explanation: 'Tracking already-consumed assertion IDs and enforcing a tight `NotOnOrAfter` window closes the replay window even if an assertion is captured in transit.' },
  { id: 'saml-10', format: 'saml-tamper', prompt: 'A SAML metadata file was fetched once during setup and never refreshed, even after the IdP rotated its signing certificate 6 months later. What operational risk does this create?', choices: ['Legitimate logins may start failing once the SP is validating against a stale, now-invalid certificate', 'The IdP will stop issuing assertions', 'SAML will silently switch to OIDC', 'No risk — SAML certificates never rotate'], correctIndex: 0, explanation: 'SAML federation trust is certificate-based. If the SP never refreshes IdP metadata, a routine key rotation on the IdP side breaks signature validation for every subsequent login until metadata is manually updated.' },
  { id: 'saml-11', format: 'saml-tamper', prompt: 'An attacker submits a SAML Response containing two `<Assertion>` elements — one properly signed, one unsigned and forged — and the SP happens to process the last one in document order. What is the underlying flaw?', choices: ['The SP processes assertion order without re-validating which one was actually signed', 'SAML disallows multiple assertions by design', 'The forged assertion cannot be valid XML', 'This is not a real attack pattern'], correctIndex: 0, explanation: 'This is another Signature Wrapping variant — an SP must ensure the assertion it acts on is the exact one covered by the verified signature, not just "a signature exists somewhere in this response."' },
  { id: 'saml-12', format: 'saml-tamper', prompt: 'A SAML `<Issuer>` element was changed from "https://real-idp.example.com" to "https://real-idp.example.com.attacker.com", and the SP\'s issuer check used a "starts with" string comparison. What went wrong?', choices: ['Using a substring/prefix match instead of an exact string comparison for a security-critical identity check', 'XML does not support subdomains', 'The signature covers only the NameID', 'SAML requires HTTPS issuers'], correctIndex: 0, explanation: 'A "starts with" comparison against attacker-controlled input is a classic mistake — always require an exact match against the expected issuer string for a trust decision like this.' },

  // --- protocol-guess ---
  { id: 'proto-1', format: 'protocol-guess', prompt: 'Guess the protocol from these progressively revealing clues.', clues: ['I was standardized in RFC 6749.', 'I define four core "grant types" for obtaining an access token.', 'I am often confused with authentication, but I only handle delegated authorization.'], choices: ['SAML 2.0', 'OAuth 2.0', 'Kerberos', 'LDAP'], correctIndex: 1, explanation: 'OAuth 2.0 (RFC 6749) is an authorization framework — it grants scoped access tokens but says nothing about authenticating who the user is (that\'s what OpenID Connect layers on top).' },
  { id: 'proto-2', format: 'protocol-guess', prompt: 'Guess the protocol from these progressively revealing clues.', clues: ['I use XML-based assertions.', 'I predate OAuth by roughly a decade.', 'My most famous real-world attack is called "Golden" me.'], choices: ['OpenID Connect', 'SCIM', 'SAML 2.0', 'WebAuthn'], correctIndex: 2, explanation: 'SAML 2.0 uses XML assertions for federated SSO, and "Golden SAML" (forging assertions with a stolen IdP signing key) is one of its most notorious real-world attacks.' },
  { id: 'proto-3', format: 'protocol-guess', prompt: 'Guess the protocol from these progressively revealing clues.', clues: ['I run on UDP port 1812 by default.', 'I combine Authentication, Authorization, and Accounting into one exchange.', 'I am still widely used for network device (802.1X) login today.'], choices: ['TACACS+', 'RADIUS', 'Kerberos', 'OCSP'], correctIndex: 1, explanation: 'RADIUS bundles AAA into a single protocol exchange and remains the dominant standard for network access authentication decades after its introduction.' },
  { id: 'proto-4', format: 'protocol-guess', prompt: 'Guess the protocol from these progressively revealing clues.', clues: ['I separate Authentication, Authorization, and Accounting into distinct packet exchanges.', 'I typically run over TCP, not UDP.', 'Network administrators use me to authorize specific commands like "configure terminal" separately from login.'], choices: ['RADIUS', 'TACACS+', 'Diameter', 'Shibboleth'], correctIndex: 1, explanation: 'Unlike RADIUS\'s combined AAA, TACACS+ separates each phase, which is exactly what allows fine-grained, per-command authorization on network devices.' },
  { id: 'proto-5', format: 'protocol-guess', prompt: 'Guess the protocol from these progressively revealing clues.', clues: ['I add an identity layer on top of an existing authorization framework.', 'My tokens are JWTs by convention.', 'I introduced the "ID Token" as distinct from an access token.'], choices: ['SAML 2.0', 'OpenID Connect', 'SCIM 2.0', 'FIDO2'], correctIndex: 1, explanation: 'OpenID Connect (OIDC) is built directly on top of OAuth 2.0, adding the ID Token (a JWT proving who the user is) that plain OAuth never defined.' },
  { id: 'proto-6', format: 'protocol-guess', prompt: 'Guess the protocol from these progressively revealing clues.', clues: ['I use symmetric-key cryptography and time-limited "tickets."', 'I involve a "Ticket Granting Ticket" issued by a Key Distribution Center.', 'Windows Active Directory relies on me heavily for domain authentication.'], choices: ['Kerberos', 'RADIUS', 'SAML', 'OAuth 2.0'], correctIndex: 0, explanation: 'Kerberos\'s ticket-based model (AS-REQ/TGT, then TGS-REQ/service tickets) is the backbone of Active Directory domain authentication.' },
  { id: 'proto-7', format: 'protocol-guess', prompt: 'Guess the protocol from these progressively revealing clues.', clues: ['I standardize how to provision and deprovision user accounts across systems.', 'I define a RESTful JSON schema for User and Group resources.', 'HR-to-IdP automation pipelines rely on me to sync joiner/mover/leaver events.'], choices: ['SCIM 2.0', 'LDAP', 'RADIUS', 'WebAuthn'], correctIndex: 0, explanation: 'SCIM (System for Cross-domain Identity Management) is the standard REST/JSON protocol for automated user lifecycle provisioning between an HR system and downstream apps/IdPs.' },
  { id: 'proto-8', format: 'protocol-guess', prompt: 'Guess the protocol from these progressively revealing clues.', clues: ['I rely on public-key cryptography and hardware-backed authenticators.', 'My credentials never leave the user\'s device.', 'I underpin what most people now call "passkeys."'], choices: ['WebAuthn / FIDO2', 'RADIUS', 'Kerberos', 'SCIM'], correctIndex: 0, explanation: 'WebAuthn (part of the FIDO2 standard) is the browser API behind passkeys — a private key never leaves the authenticator, and the server only ever sees a signed challenge.' },
  { id: 'proto-9', format: 'protocol-guess', prompt: 'Guess the protocol from these progressively revealing clues.', clues: ['I define a binary protocol, not a RESTful one.', 'I organize entries in a hierarchical tree using distinguished names (DNs).', 'Active Directory is one of my most common real-world implementations.'], choices: ['LDAP', 'SCIM', 'OAuth 2.0', 'SAML'], correctIndex: 0, explanation: 'LDAP (Lightweight Directory Access Protocol) organizes identities in a tree structure addressed by Distinguished Names — Active Directory is built on it.' },
  { id: 'proto-10', format: 'protocol-guess', prompt: 'Guess the protocol from these progressively revealing clues.', clues: ['I let a client "prove possession" of a private key alongside a bearer token.', 'I mitigate stolen-token replay by cryptographically binding the token to the requester.', 'I am defined in RFC 9449.'], choices: ['DPoP', 'PKCE', 'JARM', 'mTLS'], correctIndex: 0, explanation: 'DPoP (Demonstrating Proof-of-Possession, RFC 9449) binds an access token to a client-held key pair, so a stolen bearer token alone is no longer sufficient to use it.' },
  { id: 'proto-11', format: 'protocol-guess', prompt: 'Guess the protocol from these progressively revealing clues.', clues: ['I add an extra code-exchange step for public OAuth clients that can\'t keep a secret.', 'I use a "code verifier" and a "code challenge."', 'Mobile and single-page apps rely on me to prevent authorization-code interception.'], choices: ['PKCE', 'DPoP', 'SAML Artifact Binding', 'SCIM Bulk'], correctIndex: 0, explanation: 'PKCE (Proof Key for Code Exchange) protects public clients that can\'t safely store a client secret, closing the authorization-code-interception gap OAuth 2.0 alone left open.' },
  { id: 'proto-12', format: 'protocol-guess', prompt: 'Guess the protocol from these progressively revealing clues.', clues: ['I let an identity provider push real-time security events to relying parties.', 'I model a publish/subscribe relationship, not a simple request/response.', 'I am also known as the Shared Signals Framework.'], choices: ['CAEP / SSF', 'RADIUS', 'Kerberos', 'SCIM'], correctIndex: 0, explanation: 'Continuous Access Evaluation Protocol (CAEP), part of the Shared Signals Framework (SSF), lets an IdP proactively push events (like a session revocation) to subscribed relying parties instead of waiting for the next token refresh.' },
]
